# SecretBox

本地加密密码管理器。条目由随机数据密钥（DEK）加密后存储在本地，主密码与恢复密钥分别包装 DEK、均不落盘；用户通过桌面应用管理自己的密码条目。

## Language

### 凭据与会话

**主密码**:
用户设置的用于日常解锁的秘密，从不持久化存储。
_Avoid_: 登录密码、PIN、passphrase

**恢复密钥**:
首次设置时强制生成并要求用户离线保存的高熵随机码；忘记主密码时凭它解密数据并重设主密码。与主密码地位平等——丢失恢复密钥同样无法找回数据。
_Avoid_: 恢复码、密保问题、找回密码、后门

**锁定**:
会话的受保护状态：数据在磁盘上仍加密，必须重新输入主密码才能访问。
_Avoid_: 退出、注销

**解锁**:
用主密码解除锁定、获得数据访问权的一次会话动作。

**自动锁定**:
无操作一段时间后自动进入锁定状态的机制。

### 数据

**条目**:
一条密码记录，包含账号、密码、分类、备注。
_Avoid_: 记录、密码项、credential

**分类**:
条目上的分组标签。

**历史版本**:
条目内容每发生一次真实修改就自动留存的一个旧版本，可查看、恢复或删除。内容没有实际改动的保存不算修改，既不留下版本，也不刷新条目的修改时间。
_Avoid_: 快照（快照另有所指，见下）

**快照**:
包含全部条目及其历史版本的加密导出文件，用于备份与跨设备迁移。
_Avoid_: 备份文件、导出包、dump

**明文导出**:
把全部条目（仅当前版本，不含历史版本）一键导出为不加密 CSV 文件的操作，用于迁往其他密码管理器或用表格软件通览数据。文件不加密、不可再导入——还原与备份的语义只属于快照。
_Avoid_: 明文备份、不加密备份（"备份"的还原语义只属于快照）；CSV 导出（格式是实现选择，不是概念名）

**清除痕迹**:
导出快照之后，删除本地全部数据（含历史）的操作。

### 使用方式

**便携模式**:
将数据库文件放在用户指定位置（而非默认数据目录）的使用方式。
