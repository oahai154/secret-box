# 07 — 删除 v1 写路径（contract 收缩）

**What to build:**
v1 直派生加密的写路径全部删除：新库建立、条目写入、改主密码、导出快照只走 v2。v1 仅保留只读导入源身份（升级向导与旧快照导入使用），不存在任何"兼容写"后门。相关旧测试清理。依据 ADR-0003。

**Blocked by:** 06 — 存量 v1 库强制升级向导

**Status:** done

> 实施说明：
> - 写入口收缩：
>   - `setup_master_password` 增加核心层守卫——已设置主密码的库（v1/v2 皆然）
>     拒绝再次设置，杜绝"对存量 v1 库静默覆盖"的兼容写后门（新增回归测试
>     `已设主密码的库拒绝再次设置`，覆盖 v2 与 v1 fixture 两侧）。
>   - 条目写路径（create/update/restore）全部只经会话密钥（DEK）加密；改密、
>     设恢复密钥在 v1 库上均报 `LegacyReadOnly`（01 已落）。
>   - 应用层边界：legacy 库在渲染树中被升级向导拦截（06），导入 v1 快照后
>     `handleSessionReset` 重拉 status 即刻进入向导——UI 上不存在任何可达的
>     v1 写路径；`unlock` 的 v1 直接派生回退仅作只读导入源（升级向导收割数据用）。
> - Go 差分遗留清理：删除 `tests/crud.rs`（整个文件为 SECRETBOX_CRUD_OUT 门控
>   的 Go 回环）；移除 versions.rs、golden.rs、snapshot.rs 中各自的 Go 写盘/
>   导入门控测试与头部说明。黄金样本测试保留两项新职责：KDF/密文布局向量锁定
>   加密原语、v1 只读解锁路径读语义回归。
> - 文档：README 重写加密模型（DEK + 双钥包装、恢复密钥、秒级改密、v2 快照）、
>   新增"旧版本数据库升级"章节、更新数据安全说明（双钥丢一可救、双丢不可救）；
>   CONTEXT.md 首段同步。ADR-0003 状态 accepted，0002 标注 superseded。

- [x] v1 直派生加密的建库/写条目/改主密码/导出快照代码路径全部移除
- [x] v1 只读导入能力保留：升级向导与旧快照导入仍可用（测试覆盖）
- [x] 全量测试与构建通过，无残留的兼容写入口
- [x] 文档（README/ADR 状态）与实际能力一致
